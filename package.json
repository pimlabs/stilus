{
  "name": "stilus",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter='*' build",
    "bundle": "cd packages/mcp && bun build --compile src/index.ts --outfile ../../stilus-mcp"
  }
}
