{
  "name": "stilus",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter='*' build",
    "bundle": "bun run --filter='@pimlabs/stilus-mcp' bundle && bun run --filter='@pimlabs/stilus' bundle"
  }
}
