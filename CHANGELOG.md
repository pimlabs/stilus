# Changelog

## Unreleased

- Baseline CLI package `manuscript`.
- Added `inspect`, `clean`, and `compare` commands.
- Added JSON report support through `inspect --json`.
- Added clean manifest output at `dist/manifest.json`.
- Added compatibility wrappers through `build.py` and `analyze.py`.
- Added `data/example.md` as a tracked sample input.
- Added unit tests for cleaner, analyzer, and CLI behavior.
- Kept local manuscript data ignored through `.gitignore`.
